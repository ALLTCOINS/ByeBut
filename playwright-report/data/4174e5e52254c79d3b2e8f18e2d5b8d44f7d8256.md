# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: downloads.spec.ts >> Downloads Page >> should have silent installation section
- Location: e2e\downloads.spec.ts:36:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByText('Instalación silenciosa')
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByText('Instalación silenciosa')

```

```yaml
- paragraph: Acceso seguro
- heading "Volvé al panel con el mismo estándar soberano." [level=1]
- paragraph: Iniciá sesión para retomar control, revisar misiones, activar reglas y seguir con el estado real de tu red.
- text: Sesión conectada con Supabase SSR. Dashboard familiar, escolar y corporativo unificados. Alertas y pagos sincronizados con tu cuenta.
- paragraph: Bienvenido de vuelta
- heading "Iniciar sesión" [level=2]
- text: Control soberano Correo electrónico
- textbox "Correo electrónico":
  - /placeholder: tu@correo.com
- text: Contraseña
- textbox "Contraseña":
  - /placeholder: ••••••••
- button "Entrar"
- paragraph:
  - text: ¿No tienes cuenta?
  - link "Regístrate aquí":
    - /url: /registro
- alert
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Downloads Page', () => {
  4  |   test.beforeEach(async ({ page }) => {
  5  |     // Mock authentication for E2E tests only
  6  |     await page.goto('/');
  7  |     await page.evaluate(() => {
  8  |       localStorage.setItem('supabase.auth.token', JSON.stringify({
  9  |         access_token: 'test-token',
  10 |         refresh_token: 'test-refresh',
  11 |         user: { id: 'test-user-id', email: 'test@example.com' }
  12 |       }));
  13 |     });
  14 |   });
  15 | 
  16 |   test('should load downloads page', async ({ page }) => {
  17 |     await page.goto('/downloads');
  18 |     await expect(page.getByText('Downloads')).toBeVisible();
  19 |   });
  20 | 
  21 |   test('should show Windows download card', async ({ page }) => {
  22 |     await page.goto('/downloads');
  23 |     await expect(page.getByText('Windows')).toBeVisible();
  24 |   });
  25 | 
  26 |   test('should show macOS download card', async ({ page }) => {
  27 |     await page.goto('/downloads');
  28 |     await expect(page.getByText('macOS')).toBeVisible();
  29 |   });
  30 | 
  31 |   test('should show Linux download card', async ({ page }) => {
  32 |     await page.goto('/downloads');
  33 |     await expect(page.getByText('Linux')).toBeVisible();
  34 |   });
  35 | 
  36 |   test('should have silent installation section', async ({ page }) => {
  37 |     await page.goto('/downloads');
> 38 |     await expect(page.getByText('Instalación silenciosa')).toBeVisible();
     |                                                            ^ Error: expect(locator).toBeVisible() failed
  39 |   });
  40 | 
  41 |   test('should have hash verification section', async ({ page }) => {
  42 |     await page.goto('/downloads');
  43 |     await expect(page.getByText('Verificación de integridad')).toBeVisible();
  44 |   });
  45 | });
  46 | 
```